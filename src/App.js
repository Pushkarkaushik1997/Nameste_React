const heading=React.createElement("h1",{},"Hello world");  //properties we cam add like {id:"title"}
console.log(heading)
const root = ReactDOM.createRoot(document.getElementById("root"));
console.log(root)
root.render(heading)